# Life 项目实施基线 v0.1

更新时间：2026-08-25

后续生产发布推进和状态更新统一以 `outputs/Life-生产发布差距审计与推进总清单-v0.1.md` 为总控清单；任何完成状态必须附当前源码、自动化测试、移动端或目标环境证据。

## 1. 已确认的产品边界

- 只做移动端 PWA；不建设桌面端。
- 四个一级入口：家庭空间、吃什么、财务、更多。
- HowToCook 是“吃什么”的子页面和菜谱数据库来源，不是独立一级导航。
- 每个用户只能属于一个家庭；家庭之间的数据、AI 配置、AI 记忆文件完全隔离。
- 首发部署到腾讯云轻量服务器；正式数据使用 PostgreSQL，原始账单文件保留一年。
- 财务 V1 必须首发完整上线：预算多环、收入/支出趋势、实物资产成本趋势、全部展示项可下钻到账单/资产事件。
- 账单导入需要识别非首行表头、统一字段映射、银行与支付宝/微信等来源的关联去重；银行作为正式账本锚点，支付平台保留更详细的来源信息。
- 家庭空间以主题/想法/需求/灵感发布和家庭讨论为核心；不建设无明确需求的收件箱体系。
- 小兔子是全局 AI 入口。AI 先给解释、摘要和建议；任何写入动作都必须经过预览、确认、权限校验和审计。

## 2. 当前工程事实

| 领域 | 当前事实 | 结论 |
|---|---|---|
| 后端 | Fastify + TypeScript；财务概览、服务端下钻、统一账本、手动记账、账户/分类/预算、实物资产、成员权限、审计、导入状态机、导出队列、原始数据清理、家庭 AI 配置和 HttpOnly 身份会话均已进入实现与测试 | 原生 PostgreSQL、COS、外部 AI 和腾讯云部署尚未 live smoke |
| 数据库 | PostgreSQL 迁移已覆盖租户、财务导入、统一账本、预算、实物资产、权限、导出、AI 连接/记忆、会话和 RLS；0001–0010 已纳入迁移 runner | 当前自动化使用 PGlite 兼容验证，尚未在目标原生 PostgreSQL 实例执行 |
| API | OpenAPI 已声明 49 个路径、68 个 schema；财务核心、认证登录/注册/退出、`/api/me` 和导出/AI 配置已接入 | 以运行时注册路由和测试为验收标准，契约声明不等于目标环境 live 通过 |
| 前端 | `ui/app/` 已有 React/TypeScript 移动端骨架；家庭空间、财务首页、记账、账户/预算、资产、权限、导入/关联审核和 AI 入口已按切片接入 | 吃什么/更多仍需按后续切片完成；所有页面继续复用统一设计系统 |
| 设计系统 | 总体 UI 设计系统已收口为淡粉淡紫 + iOS 层级 + Liquid Glass 功能层，并补齐自适应容器、多预算重排、容器感知字号和长内容规则 | 页面垂直切片只能复用统一令牌、Adaptive Layout 原语和组件行为 |
| 测试 | API 14/14、类型检查、OpenAPI 校验、迁移烟测、真实四份账单回放和 430/320 移动端验收均可运行 | 原生隔离 PostgreSQL、COS/AI live smoke、正式会话和服务器部署仍是发布闸门 |
| CI | GitHub Actions `quality-gate` 已在 `e5a598e` 落地，自动执行 `npm ci`、类型检查、API 测试、OpenAPI、迁移 smoke 和 Web 构建 | 当前仓库尚无 remote，CI 尚未在远端实际触发，也尚未设置为 `main` 的 required check |
| 版本控制 | 已建立首次基线提交 `8bd7793`；分支、提交、审查和合并规范已在 `18051e0` 落地 | 当前仓库尚无 remote，`main` 远端保护待绑定仓库后验证 |

## 3. 当前主要风险

1. COS 适配器、私有对象路径、签名 URL 和清理 worker 已实现，但尚未使用腾讯云真实凭据执行 live smoke。
2. 财务核心垂直切片已通过 PGlite 和移动端验证；目标原生 PostgreSQL、连接池和部署参数仍未复验。
3. 正式 HttpOnly 会话、注册/登录/退出和 `/api/me` 已接入；目标环境仍需执行真实会话、HTTPS Cookie 和跨家庭回归。
4. 生产 AI Provider、家庭密钥引用和 AI 记忆清理已实现 fail-closed 契约，但外部 Endpoint、限流、超时和真实错误回归尚未执行。

## 4. 本轮实施顺序

### Phase 0：基线

- 固化本文件和 README。
- 保持旧版 PRD 只读，不删除历史材料。
- 以测试和实际路由作为“已完成”证据。

### Phase 1：平台基础

- 家庭主题、评论、AI 洞察、AI 行动提案和执行审计的 PostgreSQL 模型及 RLS。
- 家庭空间 API 与小兔子 AI 第一条垂直切片：主题详情 → 摘要 → 来源 → 行动提案 → 确认/拒绝 → 审计。
- 移动端前端请求层、路由、设计令牌、家庭空间骨架和财务首页基础闭环。

### Phase 2：财务 V1 收口

- 完成 COS 原始账单上传、解析器 worker 调用和批次自动推进；已确认重复按银行锚定去重，平台来源保留。
- 增加账单详情运行时路由。
- 把预算环、趋势、资产成本、导入和关联审核全部接到真实 API，并保留数据来源关系。

### Phase 2.5：财务生产闸门

- 正式导出、异步队列 worker、短期下载有效期和过期对象清理已完成。
- 原始账单与 AI 记忆一年清理、审计和正式账本保留已完成。
- 仍需在目标 PostgreSQL、COS、外部 AI、HttpOnly 会话和腾讯云轻量服务器上完成 live smoke、备份恢复和回滚演练。

### Phase 3：吃什么 → HowToCook

- 推荐、搜索、自动历史、长期偏好、菜谱详情、食材清单和采购导出。
- HowToCook 最新本地解析包作为来源；不得让 AI 生成内容冒充来源菜谱。

### Phase 4：发布硬化

- 原生 PostgreSQL/RLS 验证、文件保留清理、备份恢复、日志和告警。
- 真机/移动浏览器验收、权限回归和部署演练。

## 5. 垂直切片验收标准

后续所有页面必须遵循 `outputs/Life-页面垂直切片实施与验收流程-v0.1.md`，按“页面立项 → 产品/交互 → API/迁移 → 实现 → 自动化测试 → 移动端浏览器验收 → 回归 → 交付记录”逐页推进。未满足 Definition of Done 的页面不进入下一页面开发。

总体 UI 设计系统以 `outputs/Life-移动端总体UI设计系统-v0.1.md` 为唯一视觉基线，页面不得自行创建近似颜色、圆角、间距、玻璃层、字号缩放或状态反馈。多预算、多卡片和长金额必须使用统一自适应布局原语，不能通过页面级固定宽度或绝对定位解决。

第一条切片不以“页面能打开”为完成，而以以下闭环为完成：

1. 家庭成员可以看到只属于当前家庭的主题列表。
2. 可以发布主题和评论，并在数据库中保留作者、家庭和审计信息。
3. 小兔子生成的摘要明确标示来源主题/评论、生成时间和 Provider。
4. AI 写入建议先以提案展示；确认后才写入评论，拒绝后不得产生业务写入。
5. 越权家庭请求、儿童无授权的敏感 AI 写入、重复确认和过期提案都被拒绝。
